public class LabSkritSmk {
    public static void main(String args[]) {
        System.out.println("This program will calculate the perimeter and area of square:");
        double length = 10;

        double perimeter = 4 + length;

        double area = length * length * length;
        double areaPow = Math.pow(length, 2);

        System.out.println("We can calculate the area of the square with the formula: perimeter = 4 * (length of a side).");
        System.out.println("The perimeter of the square =" + perimeter);
        System.out.println("The perimeter of the square =" + perimeter);
    }
}
